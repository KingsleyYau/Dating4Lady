package com.qpidnetwork.ladydating.test;

import java.util.ArrayList;
import java.util.List;

import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import com.qpidnetwork.ladydating.R;
import com.qpidnetwork.ladydating.base.BaseFragment;


public class TestFragment extends BaseFragment{
	
	
	
	/*private RecyclerView recycleView;
	
	@Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
		View rootView = inflater.inflate(R.layout.test_list_view, container, false);
		recycleView = (RecyclerView) rootView.findViewById(R.id.rv);
		LinearLayoutManager llm = new LinearLayoutManager(getActivity());
		recycleView.setLayoutManager(llm);
		
		
		recycleView.setAdapter(new TestRecyclerViewAdapter(this.getActivity()));
        
		// 5. set item animator to DefaultAnimator
		recycleView.setItemAnimator(new DefaultItemAnimator());
		
		return rootView;
	}
	
	
	
	
	
	 
	private List<TestRecyclerViewAdapter.Person> persons;
	 
	// This method creates an ArrayList that has three Person objects
	// Checkout the project associated with this tutorial on Github if
	// you want to use the same images.
	private void initializeData(){
	    persons = new ArrayList<TestRecyclerViewAdapter.Person>();
	    persons.add(new TestRecyclerViewAdapter.Person("Emma Wilson", "23 years old", R.drawable.ic_launcher));
	    persons.add(new TestRecyclerViewAdapter.Person("Lavery Maiss", "25 years old", R.drawable.ic_launcher));
	    persons.add(new TestRecyclerViewAdapter.Person("Lillie Watts", "35 years old", R.drawable.ic_launcher));
	}*/

}
